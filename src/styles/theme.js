import { fontSize, fontWeight } from '<variables>/font';
import { space } from '<variables>/space';

import { colors } from '<variables>/colors';

export default {
  fontWeight,
  fontSize,
  colors,
  space
};
